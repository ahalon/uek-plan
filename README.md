# uek-plan


## 🚀 Jak uruchomić projekt

### 1. Backend (FastAPI)
Upewnij się, że masz zainstalowane wymagane biblioteki (`fastapi`, `uvicorn`, `requests`, `beautifulsoup4`, `python-dotenv`).

```bash

# Uruchom serwer API
uvicorn api:app --reload --host 0.0.0.0 --port 8000